import { CategoryEnumTranslatePipe } from './category-enum-translate.pipe';

describe('CategoryEnumTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryEnumTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
